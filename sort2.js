//neste exemplo de sort vamos utiliza-lo para organizarmos uma lista de numeros
//de forma crescente
//tambem pode ser usada de forma decrescente, adicionando um if
const numeros = [4, 3, 2, 1];
numeros.sort();
console.log(numeros);

