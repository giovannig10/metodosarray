//10 metodos array
//.concat ()
// o metodo concat serve para combinar varios arrays em um so array
// no metodo abaixo vamos concatenar valores em um array
const nomes = ['giovanni', 'ana', 'julia'];

const numeros = nomes.concat(1, [2, 3]);

console.log(numeros);